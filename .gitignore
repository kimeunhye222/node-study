# 1. 라이브러리 (가장 무겁고 깃허브에 올리면 안 되는 것)
node_modules/
jspm_packages/

# 2. 로그 파일 (에러 기록들)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# 3. 환경 변수 (비밀번호, DB 접속 주소 등 - 절대 올리면 안 돼요!)
.env
.env.test
.env.production

# 4. 빌드 결과물 (배포할 때 생기는 임시 파일)
dist/
build/
out/

# 5. 에디터 설정 (VS Code나 OS에서 만드는 개인 설정)
.vscode/
.idea/
.DS_Store
Thumbs.db

# 6. 패키지 매니저의 캐시
.npm
.eslintcache